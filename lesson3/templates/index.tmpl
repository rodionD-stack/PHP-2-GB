<html>
  <head>
  <title>{{title}}</title>
  <link rel="stylesheet" type="text/css" href="templates/css/style.css">
  </head>
  <body>
    <h2>{{title}}</h2>
     {% for photo in photos %}
        <div class="small_photo"><a Href="photo.php?photo={{photo}}"><img width='300px' src="{{path_to_photo_small}}/{{photo}}"></a></div>
      {% endfor %}
	  
	  <?="welcome"?>
  </body>
</html>